# Copy this file to .env and fill in real values as later phases require them.
# .env is git-ignored — never commit real credentials.

# Phase 2 (storage layer) - matches docker-compose.yml's default credentials
DATABASE_URL=postgresql://crypto_pulse:crypto_pulse@localhost:5432/crypto_pulse

# Phase 5 (WebSocket fan-out cache, added later)
REDIS_URL=redis://localhost:6379/0
