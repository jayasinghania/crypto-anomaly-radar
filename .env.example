# Copy this file to .env and fill in real values as later phases require them.
# .env is git-ignored — never commit real credentials.

# Phase 2 (storage layer)
DATABASE_URL=postgresql://user:password@localhost:5432/crypto_pulse

# Phase 5 (WebSocket fan-out cache, added later)
REDIS_URL=redis://localhost:6379/0
