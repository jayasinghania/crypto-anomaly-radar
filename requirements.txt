# Dependencies are added phase by phase, as each one is actually needed.

# Phase 1 (ingestion)
httpx==0.27.2

# Phase 2 (storage)
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.13.2
python-dotenv==1.0.1
